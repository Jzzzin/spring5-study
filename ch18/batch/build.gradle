dependencies {
    compile db.h2, db.dbcp2
}

jar {
    manifest {
        attributes(
                "Main-Class": "ch18.SingerJobDemo",
                "Class-Path": configurations.compile.collect { it.getName() }.join(' '))
    }
}