dependencies {
    compile spring.rabbit
}

jar {
    manifest {
        attributes(
                "Main-Class"         : "ch12.AmqpRpcDemo",
                "Class-Path"         : configurations.compile.collect { it.getName() }.join(' '))
    }
}