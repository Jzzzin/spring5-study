dependencies {
    compile spring.context
}

jar {
    manifest {
        attributes(
                "Main-Class"         : "ch11.AsyncTaskDemo",
                "Class-Path"         : configurations.compile.collect { it.getName() }.join(' '))
    }
}