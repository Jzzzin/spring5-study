dependencies{
    compile misc.javaEl, misc.glassfishEl
}

jar {
    manifest {
        attributes(
                "Main-Class"         : "ch10.Jsr349AssertTrueDemo",
                "Class-Path"         : configurations.compile.collect { it.getName() }.join(' '))
    }
}