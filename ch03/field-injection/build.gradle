jar {
    manifest {
        attributes(
                "Main-Class" : "ch3.annotated.FieldInjection",
                "Class-Path" : configurations.compile.collect { it.getName() }.join(' ')
        )
    }
}
