jar {
    manifest {
        attributes(
                "Main-Class" : "ch3.HierarchicalAppContextUsage",
                "Class-Path" : configurations.compile.collect { it.getName() }.join(' ')
        )
    }
}
