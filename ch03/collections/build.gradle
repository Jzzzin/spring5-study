jar {
    manifest {
        attributes(
                "Main-Class" : "ch3.annotated.CollectionInjection",
                "Class-Path" : configurations.compile.collect { it.getName() }.join(' ')
        )
    }
}
