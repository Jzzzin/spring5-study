apply plugin: 'war'

war {
    archiveName = 'websocket-echo.war'
    manifest {
        attributes(
                "Class-Path"         : configurations.compile.collect { it.getName() }.join(' '))
    }
}
