jar {
    manifest {
        attributes(
                "Main-Class" : "ch4.DestructiveBeanWithHook",
                "Class-Path" : configurations.compile.collect { it.getName() }.join(' ')
        )
    }
}
