jar {
    manifest {
        attributes(
                "Main-Class" : "ch4.MessageDigestFactoryDemo",
                "Class-Path" : configurations.compile.collect { it.getName() }.join(' ')
        )
    }
}
