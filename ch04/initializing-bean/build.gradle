jar {
    manifest {
        attributes(
                "Main-Class" : "ch4.SingerWithInterface",
                "Class-Path" : configurations.compile.collect { it.getName() }.join(' ')
        )
    }
}
