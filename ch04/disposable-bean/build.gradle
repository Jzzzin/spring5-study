jar {
    manifest {
        attributes(
                "Main-Class" : "ch4.DestructiveBeanWithInterface",
                "Class-Path" : configurations.compile.collect { it.getName() }.join(' ')
        )
    }
}
