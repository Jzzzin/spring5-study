dependencies {
    testCompile spring.test, testing.junit
}

jar {
    manifest {
        attributes(
                "Main-Class" : "ch4.ProfileXmlConfigDemo",
                "Class-Path" : configurations.compile.collect { it.getName() }.join(' ')
        )
    }
}
